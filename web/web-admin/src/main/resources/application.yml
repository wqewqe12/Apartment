spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://localhost:3306/lease?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=GMT%2b8
    username: root
    password: root
    hikari:
      connection-test-query: SELECT 1 # 自动检测连接
      connection-timeout: 60000 #数据库连接超时时间,默认30秒
      idle-timeout: 500000 #空闲连接存活最大时间，默认600000（10分钟）
      max-lifetime: 540000 #此属性控制池中连接的最长生命周期，值0表示无限生命周期，默认1800000即30分钟
      maximum-pool-size: 12 #连接池最大连接数，默认是10
      minimum-idle: 10 #最小空闲连接数量
      pool-name: SPHHikariPool # 连接池名称
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
#  data:
#    redis:
#      host: 192.168.20.128
#      port: 6379
#      database: 0
#      password: 123321

#用于打印框架生成的sql语句，便于调试
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      update-strategy: not_empty
      logic-delete-field: flag
      logic-delete-value: 1
      logic-not-delete-value: 0
server:
  port: 8080

springdoc:
  default-flat-param-object: true

#minio:
#  endpoint: http://192.168.20.128:9000
#  access-key: minioadmin
#  secret-key: minioadmin
#  bucket-name: apartment

qiniuyun:
  upload:
    # 七牛云访问ak
    accessKey: 8y5hbObouqEIkje9nGpRR-Kbgp0-1zdb21GjVvXA
    # 七牛云访问sk
    secretKey: cs6MRL60fCAdaS7xrjJZ4d6BEDTP8AKynWSrCyqw
    # 你自己创建的空间名
    bucket: pic12311
    # 表示在空间下穿件什么目录存储上传的图片
    mkdir: apartment/
    # 七牛云分配给你空间的域名
    domain: http://www.ctr16.xyz
